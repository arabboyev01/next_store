import {makeStyles} from "@mui/styles";

export default makeStyles(() => ({
    singleProducts: {
        maxWidth: "1230px",
        margin: "120px auto 0 auto",
    },
    productHeader: {
        display: "flex",
        flexDirection: "row",
        alignItems: "start",
    },
}))
