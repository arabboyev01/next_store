import {makeStyles} from "@mui/styles";

export default makeStyles(() => ({
    mainTitle:{
        fontSize: '28px',
        lineHeight: "33.2px",
        fontWeight: 600,
        padding: "10px 0",
    }
}))
